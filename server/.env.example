# Copy this file to .env and replace the placeholder password.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/placement_tracker

# Local PostgreSQL normally does not require SSL.
DATABASE_SSL=false
