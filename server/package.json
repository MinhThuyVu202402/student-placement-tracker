{
  "name": "student-placement-tracker-server",
  "version": "1.0.0",
  "description": "Backend API for the Student Placement Tracker",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "db:check": "node --env-file-if-exists=.env scripts/check-db.js",
    "dev": "node --env-file-if-exists=.env --watch index.js",
    "test": "node --test"
  },
  "private": true,
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
